# QuantumQuote standalone app — point these at a NEW Supabase project (NOT Marko's).
# Create a fresh Supabase project, run the migrations in supabase/migrations,
# deploy the edge functions in supabase/functions, then fill these in.
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
